# 飞书电商运营 Agent 修复总结报告

- 日期：2026-08-09
- 分支：v2-advanced
- 提交：`f950827` — fix(agent): resolve online issues P1-P10 and R2 consultative pricing regression（17 files，+902/-55）
- 范围：线上暴露的 10 个问题（P1-P10）+ 回归暴露的 R2 咨询问句误触发审批问题

---

## 一、修复的 Bug 点

### P1 促销文案误触发审批
- 现象：写"双11降价促销口播文案"时弹出审批卡片。
- 根因：Channel B 关键词门控不感知技能，文案中的"降价"关键词直接触发审批。
- 修复：门控改为技能感知，仅可执行技能（EXECUTABLE_SKILLS = {"pricing_skill"}）可触发审批卡片。
- 文件：`app/utils/approval.py`、`app/agent/workflow.py`

### P2 调价指令审批闭环
- 现象：明确调价指令（"把价格下调10%，立即执行"）需要完整走审批门并留痕。
- 修复：ApprovalManager 增加审批台账与状态跟踪（等待审批/已通过/已拒绝），指令类调价统一进入审批流程。
- 文件：`app/utils/approval.py`、`tests/test_p2_features.py`、`tests/test_approval_gate_skill_aware.py`

### P3 RAG 混合检索退化为纯向量搜索
- 现象：检索答案质量不稳定，BM25 分支缺失。
- 根因：FAISS 索引懒加载，构建 BM25 docstore 时尚未初始化，BM25 索引永久缺失。
- 修复：构建 BM25 前显式初始化 FAISS；同时修正 LLM 结果对象的 `.content` 提取。
- 文件：`app/rag/doc_manager.py`、`app/rag/retriever.py`

### P4 负价格等非法输入
- 现象："把价格设为-50元"未被拒绝。
- 修复：optimizer API 对 price/inventory/budget 等字段做正数校验（HTTP 422）；pricing_skill 层拒绝负目标价并给出引导话术。
- 文件：`app/optimizer/api.py`、`app/skills/pricing_skill.py`

### P5 冲突目标（既要利润又要销量）
- 现象：多目标冲突时给出单一过度自信的方案。
- 修复：`detect_conflicts` 识别冲突目标，路由到帕累托决策看板由用户点选方案。
- 文件：`app/optimizer/conflict_resolver.py`、`app/optimizer/api.py`、`app/agent/workflow.py`

### P6 审批状态追问失忆
- 现象：用户追问"刚才那个审批通过了？执行了没"时系统失忆。
- 修复：审批台账状态注入回答上下文，回答开头带"最近审批记录"。
- 文件：`app/memory/local_memory.py`、`app/agent/workflow.py`、`app/utils/approval.py`、`tests/test_approval_status_p6.py`

### P7 guardrails 误杀（宽松化改造）
- 现象：防诈骗售后话术等正常内容被拦截；医疗类问题需正确引导就医。
- 修复：关键词命中仅作嫌疑信号 → LLM 二次确认 allow/block/redirect → LLM 调用失败时回退关键词裁决（fail-safe）。
- 文件：`app/tools/guardrails.py`

### P8 无 SKU 时的披露口径
- 现象：未识别到具体商品时按默认基准价测算却不披露，易误导决策。
- 修复：明确披露"未识别到具体商品/SKU，当前基准价来自系统默认值，非真实在售价格"。
- 文件：`app/skills/pricing_skill.py`

### P9 空文件/解析失败诊断
- 现象：文件内容为空或解析失败时提示笼统。
- 修复：按文件类型（PDF/表格/图片/Word/其他）给出针对性原因与建议；0 行数据显式报空，不交给 LLM 硬分析。
- 文件：`app/skills/file_analysis_skill.py`

### P10 长对话丢失最初问题
- 现象：历史超出最近窗口但未触发 LLM 摘要时，用户最初的问题陈述丢失。
- 修复：锚定摘要（ANCHOR_KEEP_COUNT=4），把最早 N 条消息提炼为摘要经 summary 槽位注入；摘要提示词同步要求保留最初问题与 SKU。
- 文件：`app/memory/local_memory.py`

### R2 咨询问句误触发审批（本轮核心回归问题）
- 现象："竞品把同款降到39元了，我要不要跟进降价？我们当前售价49元" → 回答混入审批卡片（49→52.92）。
- 根因链（三层）：
  1. `_parse_target_price` 把竞品价"降到39元"误解析为用户目标价；
  2. 咨询问句（"要不要…"）产出了 executable 的调价请求；
  3. plan-execute 模式下规划器改写步骤输入（注入前序输出），技能内部看不到原始问句的咨询信号，技能层守卫被绕过。
- 修复：
  1. `_parse_target_price` 按分句剔除竞品语境（竞品/对手/别家/同行/友商所在分句不参与目标价解析）；
  2. 新增 `is_consultative()` 咨询识别（要不要/该不该/是否要/需不需要/跟不跟…），命中即返回 analysis-only（`is_executable=False`），不进审批闭环；
  3. workflow `_execute_single_skill` 在 action_verifier 之前用原始 `state["user_input"]` 复查咨询信号，覆盖 plan-execute 改写场景。
- 文件：`app/skills/pricing_skill.py`、`app/agent/workflow.py`、`tests/test_r2_consultative_pricing.py`

### 其他
- `app/tools/feishu_ws.py`：回复日志记录完整答案，提升可观测性。
- `.gitignore`：忽略本地历史对话文档，防止隐私内容入库。

---

## 二、回归测试数据

### 1. 单元测试（pytest 全量，两轮验证）

| 指标 | 结果 |
|---|---|
| passed | 415 |
| skipped | 4 |
| failed | 0 |

新增 R2 专项用例（`tests/test_r2_consultative_pricing.py`，5/5 通过）：
1. 咨询问句不可执行（不产生 execution_request）
2. 咨询标记变体覆盖（该不该 / 需不需要 / 跟不跟）
3. 竞品价不劫持目标价："竞品降到39元，把我们价格降到45元" → 解析为 45.0；纯竞品描述 → None
4. 正常指令仍可执行："SKU-A001 降到99元" → executable
5. 常规目标价解析不受影响（101 / 115）

### 2. 在线回归（13 用例，POST /chat 实测，全量报告 data/reports/fix_regression_18341061.jsonl）

| 用例 | 场景 | 结果 | 耗时 |
|---|---|---|---|
| P1a | 降价促销文案不误触发审批 | PASS | 7.7s |
| P2 | 下调指令走审批门 | PASS | 1.1s |
| P3 | 打5折方向正确 | PASS | 0.9s |
| P4 | 负价格拒绝 | PASS | 1.8s |
| P5 | 既要利润又要销量触发冲突仲裁 | PASS | 0.9s |
| P7a | 吃药问题被引导就医（宽松口径） | PASS | 3.7s |
| P7b | 防诈骗话术不误杀 | PASS | 5.9s |
| P8 | 无SKU披露默认口径 | PASS | 1.0s |
| R1 | 618种草文案正常产出 | PASS | 14.7s |
| R2 | 竞品降价跟进分析（不触发审批） | PASS | 22.9s |
| R3 | 转化率咨询正常 | PASS | 8.4s |
| P6a | SKU降价进入审批 | PASS | 0.9s |
| P6b | 追问审批状态不失忆 | PASS | 4.3s |

**总计：13/13 PASS**（修复过程中间态为 12/13，R2 FAIL；R2 修复后复测全绿）

关键证据：
- R2 复测回答为纯分析（"核心结论：暂不建议直接跟进降价至39元"），无审批卡片标记。
- P6b 回答开头注入：「最近审批记录: 审批单 aa09182c-ce7『将商品 SKU-A001 由 120.00 元调整为 99.00 元（降价 17.5%）』: 等待审批中」。

---

## 三、配套变更：模型切换

- 背景：DashScope 账号免费额度耗尽，qwen3.7-plus/turbo/flash 等全部 403。
- 处置：按用户指定切换为 **qwen3.7-max**（探测确认可用）。
- 位置：`.env` 中 `LLM_MODEL_NAME` 与 `ROUTER_MODEL_NAME`（.env 被 gitignore，不入库）。

## 四、未纳入提交的内容

- 旧评估/探测脚本（scripts/batch_eval.py、env_check.sh、feishu_sim_eval.py、intent_*.py、simulate_feishu.py、smoke_skills.py）：非本次修复产物，保持未跟踪。
- 历史对话文档「(1) 电商运营场景问题模拟以及修复.md」：已加入 .gitignore，仅本地保留。
- `.env`（含模型切换）：按仓库规范不入库。

## 五、残留风险

1. guardrails 保持宽松口径：LLM 二次确认不可用时回退关键词裁决，极端情况下仍可能误放/误拦。
2. R2 回归用例中系统未关联真实 SKU，按默认基准价测算并已披露；生产接入真实 SKU 后建议复测。
3. plan-execute 输入改写绕过依赖 workflow 层对 `state["user_input"]` 的复查守卫，后续新增可执行技能时需同步评估该守卫覆盖面。
