# ============================================================
# 依赖清单 (2026-08-30 锁定到当前验证通过的版本组合)
#
# 历史上根目录曾残留 4 个 Python 版本的编译产物, 环境漂移导致过
# 隐性行为差异。核心运行时依赖现以 == 锁定为本仓库 CI 与探针
# 验证过的版本组合; 升级时请先跑全量测试 (543 个) 再放宽。
# ============================================================

# Web Framework
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.12.4

# LLM & LangChain
langchain-core==1.5.6
langchain-openai==1.5.1
openai==3.2.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langgraph==1.2.11

# Embeddings & Vector Store
sentence-transformers==5.7.0
faiss-cpu==1.15.0

# Feishu Integration
lark-oapi==1.7.2
# pycryptodome 由 lark-oapi 作为传递依赖引入（飞书事件解密），无需单独声明

# Data Processing
pandas==3.0.5
openpyxl==3.1.5
xlrd==2.0.2
PyPDF2==3.0.1
python-docx==1.2.0

# Database
SQLAlchemy==2.0.52

# Networking
requests==2.32.5
httpx==0.28.1
parsel==1.11.0

# L4 Optimization & Crawler (优化决策层 & 市场哨兵)
numpy==2.5.2
scipy==1.18.0
optuna==4.9.0
# curl_cffi 为可选依赖(浏览器指纹伪装), 代码内 try-import 自动降级到 httpx
curl_cffi==0.16.0

# Event Bus (可选, 默认使用内存实现; 多实例部署时启用)
# redis>=5.0

# Scheduling
APScheduler==3.11.3

# Environment
python-dotenv==1.2.1

# Testing
pytest==9.1.1

# Dev Tools
flake8==7.3.0
mypy==2.3.1
bandit==1.9.4

# Hybrid Search & Rerank
rank-bm25==0.2.2
jieba==0.42.1
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
